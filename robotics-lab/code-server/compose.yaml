name: code-server

x-workspace: &workspace
  image: vex-workspace:1.0
  restart: unless-stopped
  init: true
  cpus: 0.75
  mem_limit: 768m
  pids_limit: 256
  security_opt:
    - no-new-privileges:true
  logging:
    driver: local
    options:
      max-size: 10m
      max-file: "3"
  networks: [proxy]
  environment:
    DOCKER_USER: coder
  healthcheck:
    test: ["CMD", "curl", "-fsS", "http://localhost:8080/healthz"]
    interval: 30s
    timeout: 5s
    retries: 3
    start_period: 20s

services:
  cs-1:
    <<: *workspace
    container_name: cs-1
    environment:
      PASSWORD: ${STUDENT_1_PASSWORD:?set in .env}
    volumes:
      - ./data/${STUDENT_1_NAME}:/home/coder/robot
      - /opt/robotics-lab/secrets/git/student-1:/home/coder/.ssh:ro
    labels:
      - traefik.enable=true
      - traefik.http.routers.cs-1.rule=Host(`${STUDENT_1_NAME}.${LAB_DOMAIN}`) || Host(`${STUDENT_1_NAME}.${LAB_ALIAS_DOMAIN}`)
      - traefik.http.routers.cs-1.entrypoints=websecure
      - traefik.http.routers.cs-1.tls.certresolver=letsencrypt
      - traefik.http.services.cs-1.loadbalancer.server.port=8080

  cs-2:
    <<: *workspace
    container_name: cs-2
    environment:
      PASSWORD: ${STUDENT_2_PASSWORD:?set in .env}
    volumes:
      - ./data/${STUDENT_2_NAME}:/home/coder/robot
      - /opt/robotics-lab/secrets/git/student-2:/home/coder/.ssh:ro
    labels:
      - traefik.enable=true
      - traefik.http.routers.cs-2.rule=Host(`${STUDENT_2_NAME}.${LAB_DOMAIN}`) || Host(`${STUDENT_2_NAME}.${LAB_ALIAS_DOMAIN}`)
      - traefik.http.routers.cs-2.entrypoints=websecure
      - traefik.http.routers.cs-2.tls.certresolver=letsencrypt
      - traefik.http.services.cs-2.loadbalancer.server.port=8080

  cs-3:
    <<: *workspace
    container_name: cs-3
    environment:
      PASSWORD: ${STUDENT_3_PASSWORD:?set in .env}
    volumes:
      - ./data/${STUDENT_3_NAME}:/home/coder/robot
      - /opt/robotics-lab/secrets/git/student-3:/home/coder/.ssh:ro
    labels:
      - traefik.enable=true
      - traefik.http.routers.cs-3.rule=Host(`${STUDENT_3_NAME}.${LAB_DOMAIN}`) || Host(`${STUDENT_3_NAME}.${LAB_ALIAS_DOMAIN}`)
      - traefik.http.routers.cs-3.entrypoints=websecure
      - traefik.http.routers.cs-3.tls.certresolver=letsencrypt
      - traefik.http.services.cs-3.loadbalancer.server.port=8080

  cs-4:
    <<: *workspace
    container_name: cs-4
    environment:
      PASSWORD: ${STUDENT_4_PASSWORD:?set in .env}
    volumes:
      - ./data/${STUDENT_4_NAME}:/home/coder/robot
      - /opt/robotics-lab/secrets/git/student-4:/home/coder/.ssh:ro
    labels:
      - traefik.enable=true
      - traefik.http.routers.cs-4.rule=Host(`${STUDENT_4_NAME}.${LAB_DOMAIN}`) || Host(`${STUDENT_4_NAME}.${LAB_ALIAS_DOMAIN}`)
      - traefik.http.routers.cs-4.entrypoints=websecure
      - traefik.http.routers.cs-4.tls.certresolver=letsencrypt
      - traefik.http.services.cs-4.loadbalancer.server.port=8080

  cs-5:
    <<: *workspace
    container_name: cs-5
    environment:
      PASSWORD: ${STUDENT_5_PASSWORD:?set in .env}
    volumes:
      - ./data/${STUDENT_5_NAME}:/home/coder/robot
      - /opt/robotics-lab/secrets/git/student-5:/home/coder/.ssh:ro
    labels:
      - traefik.enable=true
      - traefik.http.routers.cs-5.rule=Host(`${STUDENT_5_NAME}.${LAB_DOMAIN}`) || Host(`${STUDENT_5_NAME}.${LAB_ALIAS_DOMAIN}`)
      - traefik.http.routers.cs-5.entrypoints=websecure
      - traefik.http.routers.cs-5.tls.certresolver=letsencrypt
      - traefik.http.services.cs-5.loadbalancer.server.port=8080

  cs-6:
    <<: *workspace
    container_name: cs-6
    environment:
      PASSWORD: ${STUDENT_6_PASSWORD:?set in .env}
    volumes:
      - ./data/${STUDENT_6_NAME}:/home/coder/robot
      - /opt/robotics-lab/secrets/git/student-6:/home/coder/.ssh:ro
    labels:
      - traefik.enable=true
      - traefik.http.routers.cs-6.rule=Host(`${STUDENT_6_NAME}.${LAB_DOMAIN}`) || Host(`${STUDENT_6_NAME}.${LAB_ALIAS_DOMAIN}`)
      - traefik.http.routers.cs-6.entrypoints=websecure
      - traefik.http.routers.cs-6.tls.certresolver=letsencrypt
      - traefik.http.services.cs-6.loadbalancer.server.port=8080

  cs-coach:
    <<: *workspace
    container_name: cs-coach
    environment:
      PASSWORD: ${COACH_PASSWORD:?set in .env}
    volumes:
      - ./data/${COACH_NAME}:/home/coder/robot
      - /opt/robotics-lab/secrets/git/coach:/home/coder/.ssh:ro
    labels:
      - traefik.enable=true
      - traefik.http.routers.cs-coach.rule=Host(`${COACH_NAME}.${LAB_DOMAIN}`) || Host(`${COACH_NAME}.${LAB_ALIAS_DOMAIN}`)
      - traefik.http.routers.cs-coach.entrypoints=websecure
      - traefik.http.routers.cs-coach.tls.certresolver=letsencrypt
      - traefik.http.services.cs-coach.loadbalancer.server.port=8080

networks:
  proxy:
    external: true
